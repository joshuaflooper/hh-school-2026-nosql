## Домашнее задание по NoSQL 

### 1. Redis

1. Rate Limiter — это механизм защиты от большого количества запросов к сервису, которые могут быть вызваны различными вредоносными действиями, например, DDoS-атакой, brute force'ом и т.п. Принцип работы прост — пропустить не больше чем заданное количество запросов в заданный промежуток времени.

2. Прочитал статью, в ней были описаны различные алгоритмы. Не вдаваясь в их описание, сразу вспомнил про команду `SET ... EX`, про которую говорилось на лекции. Почитал документацию Redis'a, нашёл отдельную команду `EXPIRE` и параметр `NX` для неё, который добавляет время жизни, только если оно не было добавлено до этого. Сразу напросилась идея сделать для запросов счётчик через string, который умирает через `timeWindowsSeconds` секунд после первого запроса в рамках счётчика. Ну и соответственно при каждом запросе проверять, есть ли живой счётчик и не достигнуты ли `maxRequestCount` запросов. Если запрос проходит, то увеличиваем счётчик или создаём новый, если активного счётчика нет — с точки зрения команды INCR, найденной в той же документации, это одно и то же.<br><br>
Короче, получилось фиксированное окно из статьи. Естественно тест на скользящее окно такой алгоритм не проходит, потому что подсчёт количества выполненных запросов никак не привязан ко времени самого запроса и выполняется только внутри фиксированного периода жизни счётчика.<br><br>
То есть надо проверять количество запросов, произошедших в течение `timeWindowsSeconds` до запроса. Чтобы проверить, не равно ли оно `maxRequestCount` и можем ли мы воткнуть ещё один запрос, достаточно проверить что запрос номер `maxRequestCount` с конца был выполнен больше, чем `timeWindowsSeconds` секунд назад. Для этого отлично подойдёт list, в который мы будем записывать время выполнения запросов. При каждом запросе выполняем описанную проверку и, если запрос проходит, добавляем текущее время в список. <br><br>
В итоге получаем скользящий журнал из статьи (ну по-моему). Все тесты проходятся.<br><br>
3. См. код.

<!-- ### 2. Scylla/Cassandra

**Описание**: Cassandra/Scylla - это wide column store база данных, где важно проектировать таблицы под конкретные запросы.

**Задание**: В папке `scylla` лежит файл `schema.sql`, представляющий собой схему таблиц, как бы они выглядели в SQL.
Необходимо написать запросы для создания таблицы (или нескольких таблиц) в Cassandra/Scylla и написать CQL
запросы под следующие требования:

1. Запрос, который бы доставал **посты заданного пользователя**, упорядоченные 
   по времени создания поста (сначала новые).
   Должен возвращать: `post_id`, `content`, дату создания поста.

2. Запрос, который бы доставал **последний пост заданного пользователя**.
   Должен возвращать: `post_id`, `content`, дату создания поста, `username`.

3. Запрос, который бы доставал **посты заданного пользователя после заданной даты**.
   Должен возвращать: `post_id`, `content`, дату создания поста.

4. Запрос, который бы доставал **пользователей, сделавших пост в заданный день по заданной теме**,
   упорядоченные по времени (сначала новые).
   Должен возвращать: `user_id`, `username`, `post_id`, время создания поста.

**Решение** (таблицы и запросы) писать в файле `scylla/solution.cql`.

### 3. ClickHouse

**Описание**: ClickHouse - это колоночная аналитическая СУБД, оптимизированная для OLAP запросов и работы с большими объемами данных.

**Документация**: https://clickhouse.com/docs

**Задание**: В папке `clickhouse` находится файл `server_logs.csv` с логами веб-сервера.

1. **Создать таблицу** для хранения логов:
   - Используйте движок `MergeTree`
   - Выберите подходящий `ORDER BY` для оптимизации запросов
   - Определите типы данных для каждого поля

2. **Загрузить данные** из CSV файла в таблицу

3. **Написать аналитические запросы**:
   - **Топ-5 самых медленных endpoint'ов** (по среднему времени ответа `response_time_ms`)
   - **Количество запросов по часам** за весь период в логах
   - **Процент ошибок** (status_code >= 400) для каждого endpoint'а

**Решение** писать в файле `clickhouse/solution.sql`.

**Подсказка**: для загрузки CSV можно использовать:
```bash
cat server_logs.csv | docker exec -i clickhouse clickhouse-client --query="INSERT INTO server_logs FORMAT CSVWithNames"
``` -->
